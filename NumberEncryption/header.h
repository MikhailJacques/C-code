//This file contains the origional defination of function prototype
//It is only included with the main function


#include <stdio.h>
#include <ctype.h>

int checkPos(int pos1, int pos2);
void encrypt(unsigned int number, int pos1, int pos2);
void print(unsigned int number, unsigned int encrypted);
void readData(unsigned int *number, int *pos1, int *pos2);
